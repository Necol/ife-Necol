大体块

1 新建一个新数组默认值  随机数值

2 函数input-unshift插入第一个输入值
   1 DOM获取输入值 unshift插入数

3 函数input-pop插入最后一个值
  1 DOM获取输入值 pop插入数组

4 函数input-shift删除第一个值

5 函数input- push删除最后一个值
 
初始队列为空
需要考虑数字输入的合法性

1获取输入值的函数 返回输入的数值
2点击哪个键做相应的操作函数
3函数DOM往页面增加数组新增数值块
如果增加数值DOM块渲染

 判断数组的长度
如果数组为空 弹出提示框
数组 长度+1 则增加一个DIV  通过获取DIV数组对象的index来绑定 数组的index  实现增删


判断获取值时，可以写一个函数，isNaN()会将所有不能被转换成数字的格式强制转换为NAN（非数字，包括undefined 但null会被转换成0返回false)


appendchild 是在父元素节点的最后一个子节点后插入  

nodeValue 与Value